= render partial: "items", locals: { items: items }
= render partial: "success_message", locals: { selector: "#warning", message: t('message.register_successfully') + ' ' + l(item.action_date) + ' ' + item.name + ' ' + number_to_currency(item.amount) }
= render partial: "highlight_items", locals: { ids: updated_item_ids }
! $('#do_add_item #item_name').val('');
! $('#do_add_item #amount').val('');
! $('#do_add_item #tag_list').val('');
! $('#candidates').html('');
