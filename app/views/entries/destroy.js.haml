= render partial: 'remove_items', locals: { ids: deleted_ids }
- item_name = item.adjustment? ? t('label.adjustment') : item.name
= render partial: 'success_message', locals: { selector: '#warning', message: "#{t('message.delete_successfully')} #{l(item.action_date)} #{item_name.decorate} #{number_to_currency(item.amount)}" }
= render partial: 'replace_items', locals: { items: updated_items }
