-  partial = @item.adjustment? ? 'edit_adjustment' : 'edit_item'
:plain
  $("#item_#{@item.id}").hide();
  $("#item_#{@item.id}").html("#{j(render partial: partial, locals: {item: @item})}");
  $("#item_#{@item.id}").slideDown(#{Settings.effect.slide.duration});

