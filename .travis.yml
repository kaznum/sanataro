script: "rake travis"
before_script:
  - "cp -v config/database.yml.travis.${TRAVIS_RUBY_VERSION}.${DB} config/database.yml"
  - "bundle exec rake --trace db:create db:migrate"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

rvm:
  - jruby-head

env:
  - DB=mysql
  - DB=postgres
  - DB=sqlite

